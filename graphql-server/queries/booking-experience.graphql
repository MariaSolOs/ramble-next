query getBookingExperience($id: ID!) {
    experiencesById(ids: [$id]) {
        ...ExperienceView
        privatePrice
        currency
        creator {
            user {
                phoneNumber
            }
        }
    }
}